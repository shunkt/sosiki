# Empty template — every value here MUST be supplied by an overlay
# (overlays/local's secretGenerator, reading a local, gitignored .env file).
# Never commit real values to this file.
apiVersion: v1
kind: Secret
metadata:
  name: kaigi-secrets
  namespace: kaigi
type: Opaque
stringData:
  OPENAI_API_KEY: ""
  MINIO_ACCESS_KEY: "minioadmin"
  MINIO_SECRET_KEY: "minioadmin"
