# Node
node_modules/
dist/
*.local

# Go
/backend/bin/
*.test
coverage.out
# `go build ./cmd/x/...` (no -o) drops a binary named after the package
# directly in backend/ — these are exactly cmd/*'s directory names.
/backend/discovery
/backend/moderator
/backend/persona
/backend/seed

# Env
.env
.env.*
!.env.example

# Editors / OS
.DS_Store
.idea/
.vscode/
k8s/overlays/local/secret-patch.yaml
